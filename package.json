{
  "name": "kuveytturk-kiosk",
  "version": "1.0.0",
  "private": true,
  "description": "KuveytTürk Yapay Zeka Laboratuvarı — dikey kiosk sunucusu (sıfır bağımlılık)",
  "license": "UNLICENSED",
  "main": "server.js",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js",
    "test:coverage": "node --test --experimental-test-coverage tests/*.test.js",
    "lint": "node scripts/lint.js",
    "audit:prod": "npm audit --omit=dev --audit-level=high"
  },
  "dependencies": {},
  "devDependencies": {}
}
