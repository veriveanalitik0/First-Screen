services:
  kiosk:
    build: .
    container_name: kuveyt-turk-kiosk
    ports:
      - "5353:5353"          # TV: http://<sunucu-ip>:5353/  · Panel: /control
    volumes:
      - kiosk-data:/data     # ayarlar burada kalıcı olur (yeniden başlatmada korunur)
    restart: unless-stopped

volumes:
  kiosk-data:
